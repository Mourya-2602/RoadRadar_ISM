name: road_radar
description: "A new Flutter project."
# Prevents accidental publication to pub.dev.
publish_to: 'none'

# Application versioning.
version: 1.0.0+1

# Dart SDK environment.
environment:
  sdk: ^3.6.1

# Dependencies for your application.
dependencies:
  flutter:
    sdk: flutter
  google_maps_flutter: ^2.11.0
  geolocator: ^13.0.3
  permission_handler: ^11.4.0
  cupertino_icons: ^1.0.8

# Development dependencies.
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

# Flutter-specific configurations.
flutter:
  # Ensures Material Icons font is included.
  uses-material-design: true

  # Fonts configuration.
  fonts:
    - family: Raleway
      fonts:
        - asset: fonts/Raleway-VariableFont_wght.ttf

  # To add assets, uncomment and use the assets section:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
